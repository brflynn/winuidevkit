# HelloWorld

A minimal pywinui sample app with a button that toggles between
"Click me" and "Clicked!".

## Run

```bash
cd samples/HelloWorld
pywinui run
```

## Build

```bash
pywinui build
```

Output lands in `dist/HelloWorld/`.
