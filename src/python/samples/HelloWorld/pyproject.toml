[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "HelloWorld"
version = "0.1.0"
description = "HelloWorld – a pywinui sample app"
requires-python = ">=3.10"
dependencies = [
    "pywinui",
]
